/**
 * Temporary product switches.
 *
 * Keep the historical OKR data in Supabase, but hide the Goal/KR experience
 * while the workspace is operating in task-first mode.
 */
export const OKR_FEATURE_ENABLED = false;

